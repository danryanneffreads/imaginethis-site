<section class="page-shell">
  <div class="orb orb-one"></div>
  <div class="orb orb-two"></div>

  <header class="hero">
    <p class="eyebrow">Digital structure for thoughtful people</p>
    <h1>Build a clearer doorway into your work.</h1>
    <p class="hero-text">
      A calm, structured web presence for links, projects, ideas, conversations, and the work you want people to understand.
    </p>

    <div class="hero-actions">
      <a class="button primary" href="#start">Start exploring</a>
      <a class="button secondary" href="#systems">See the structure</a>
    </div>
  </header>

  <main>
    <section class="card-grid" id="systems">
      <article class="card">
        <span>01</span>
        <h2>Public entry point</h2>
        <p>A simple page that helps people understand who you are, what you make, and where to go next.</p>
      </article>

      <article class="card">
        <span>02</span>
        <h2>Structured thinking</h2>
        <p>Turn scattered ideas, links, updates, and projects into a coherent body of work.</p>
      </article>

      <article class="card">
        <span>03</span>
        <h2>Conversation-ready</h2>
        <p>Give people enough context to reach out, collaborate, invite, hire, or understand your direction.</p>
      </article>
    </section>

    <section class="feature" id="start">
      <div>
        <p class="eyebrow">A better starting point</p>
        <h2>Not louder. Clearer.</h2>
      </div>
      <p>
        The goal is not to perform for an algorithm. The goal is to create a durable place where your work, interests,
        and direction can become easier to see over time.
      </p>
    </section>
  </main>
</section>

<style>
  :global(body) {
    margin: 0;
    font-family:
      Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    background: #10131f;
    color: #f7efe4;
  }
<style>
  .page-shell {
    position: relative;
    min-height: 100vh;
    overflow: hidden;
    padding: 56px 24px;
    background:
      radial-gradient(circle at top left, rgba(238, 177, 104, 0.22), transparent 32%),
      radial-gradient(circle at bottom right, rgba(128, 160, 255, 0.2), transparent 34%),
      linear-gradient(135deg, #10131f 0%, #1c1b2e 55%, #271d25 100%);
  }

  .hero,
  main {
    position: relative;
    z-index: 2;
    max-width: 1100px;
    margin: 0 auto;
  }

  .hero {
    padding: 72px 0 48px;
  }

  .eyebrow {
    margin: 0 0 16px;
    color: #eeb168;
    font-size: 0.82rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  h1 {
    max-width: 820px;
    margin: 0;
    font-size: clamp(3rem, 8vw, 7.5rem);
    line-height: 0.9;
    letter-spacing: -0.07em;
  }

  .hero-text {
    max-width: 680px;
    margin: 28px 0 0;
    color: rgba(247, 239, 228, 0.78);
    font-size: clamp(1.1rem, 2vw, 1.45rem);
    line-height: 1.6;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 14px;
    margin-top: 34px;
  }

  .button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 46px;
    padding: 0 20px;
    border-radius: 999px;
    font-weight: 700;
    text-decoration: none;
  }

  .primary {
    background: #f7efe4;
    color: #10131f;
  }

  .secondary {
    border: 1px solid rgba(247, 239, 228, 0.28);
    color: #f7efe4;
  }

  .card-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 18px;
    margin-top: 24px;
  }

  .card,
  .feature {
    border: 1px solid rgba(247, 239, 228, 0.16);
    border-radius: 28px;
    background: rgba(255, 255, 255, 0.07);
    box-shadow: 0 24px 80px rgba(0, 0, 0, 0.24);
    backdrop-filter: blur(18px);
  }

  .card {
    padding: 28px;
  }

  .card span {
    color: #eeb168;
    font-weight: 800;
  }

  .card h2 {
    margin: 28px 0 12px;
    font-size: 1.35rem;
  }

  .card p,
  .feature p {
    color: rgba(247, 239, 228, 0.72);
    line-height: 1.65;
  }

  .feature {
    display: grid;
    grid-template-columns: 0.8fr 1.2fr;
    gap: 28px;
    margin-top: 18px;
    padding: 36px;
  }

  .feature h2 {
    margin: 0;
    font-size: clamp(2rem, 4vw, 4rem);
    letter-spacing: -0.05em;
    line-height: 1;
  }

  .orb {
    position: absolute;
    border-radius: 999px;
    filter: blur(12px);
    opacity: 0.45;
  }

  .orb-one {
    width: 280px;
    height: 280px;
    top: -90px;
    right: 12%;
    background: #eeb168;
  }

  .orb-two {
    width: 220px;
    height: 220px;
    bottom: 10%;
    left: -70px;
    background: #829cff;
  }

  @media (max-width: 800px) {
    .page-shell {
      padding: 32px 18px;
    }

    .hero {
      padding-top: 42px;
    }

    .card-grid,
    .feature {
      grid-template-columns: 1fr;
    }
  }
</style>