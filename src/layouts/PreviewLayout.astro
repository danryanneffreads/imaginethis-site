---
import BaseLayout from './BaseLayout.astro';
import Hero from '../components/Hero.astro';

const {
  title = "Preview",
  eyebrow = "ImagineThis Preview",
  description = "",
  status = "published"
} = Astro.props;
---

<BaseLayout title={title} status={status}>
  <Hero
    eyebrow={eyebrow}
    title={title}
    description={description}
  />

  <slot />
</BaseLayout>